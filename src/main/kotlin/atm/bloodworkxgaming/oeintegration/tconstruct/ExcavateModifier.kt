package atm.bloodworkxgaming.oeintegration.tconstruct

import slimeknights.tconstruct.library.modifiers.Modifier

/**
 * Created by jonas on 19.06.2017.
 */
class ExcavateModifier : Modifier( 0xCC6600) {
    /*
    fun canApplyTogether(enchantment: Enchantment): Boolean {
        return MainConfig.allowMendingTinkers || enchantment !== Enchantments.MENDING
    }

    fun canApplyTogether(otherModifier: IToolMod): Boolean {
        return MainConfig.allowMendingTinkers || !otherModifier.getIdentifier()
            .equals(TinkerModifiers.modMendingMoss.getIdentifier())
    }

    fun applyEffect(nbtTagCompound: NBTTagCompound?, nbtTagCompound1: NBTTagCompound?) {
        // no extra data needed
    }

    fun getTooltip(modifierTag: NBTTagCompound?, detailed: Boolean): String {
        return getLeveledTooltip(modifierTag, detailed)
    }
*/
    init {
        
/*
        this.addAspects(
            ModifierAspect.toolOnly,
            MultiAspect(this, 1, MainConfig.maxTinkersModifersCount, 1)
        )
        this.addAttributes()
         this.addItem(ModItems.ITEM_EXCAVATE_MODIFIER, 1, 1)*/
    }
}
